---
const { title = "ricadmin.io", description = "" } = Astro.props;
import "../styles/bios.css";
---
<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
  </head>
  <body>
    <main>
      <div class="nav">
       <a href={`${Astro.base}/`}>Inicio</a>
		<a href={`${Astro.base}/linux`}>Linux</a>
		<a href={`${Astro.base}/redes`}>Redes</a>
		<a href={`${Astro.base}/virtualizacion`}>Virtualización</a>
		<a href={`${Astro.base}/seguridad`}>Seguridad</a>
		<a href={`${Astro.base}/proyectos`}>Proyectos</a>
      </div>

      <hr />
      <slot />
      <hr />

      <div class="small">© 12Bit — ricadmin.io</div>
    </main>
  </body>
</html>
